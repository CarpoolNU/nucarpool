# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
# Yarn Classic is the package manager (see README). An accidental `npm install`
# would commit a second lockfile that nothing keeps in sync with yarn.lock.
package-lock.json

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files
.env*.local
.env
# Written by the Amplify build spec from the container's environment, so it
# holds real credentials whenever that build is run. `.env*.local` above does
# not cover it and `.env` matches only the exact name.
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# local DB
nucarpool-db-data/

# Claude Code
# .claude/settings.json and .claude/skills/ are intentionally tracked — they are
# the shared permission guardrails and workflow. Only per-machine overrides,
# which Claude Code writes itself when you approve a permission permanently,
# are personal. Never commit them.
.claude/settings.local.json

# Linked git worktrees, one per concurrent session, so that two sessions sharing
# this repository cannot move each other's HEAD. Create them with
# `git worktree add --no-track -b <branch> .claude/worktrees/<name> origin/main`
# — without --no-track the new branch tracks origin/main, which aims a later
# bare `git push` at main. They are checkouts, not content. Never commit them.
.claude/worktrees/
